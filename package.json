{
  "name": "bia-xinsheng-service",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "pnpm --filter bia-admin dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=20.0.0"
  }
}
